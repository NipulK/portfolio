"use client";
import { useEffect,useState } from "react";import { Github,Linkedin,Moon,Search } from "lucide-react";import { useTheme } from "./providers";
const destinations=[{label:"Home",href:"#home"},{label:"About",href:"#about"},{label:"Projects",href:"#projects"},{label:"Skills",href:"#skills"},{label:"Contact",href:"#contact"},{label:"GitHub",href:"https://github.com/NipulK"},{label:"LinkedIn",href:"https://www.linkedin.com/in/nipul-kanishka-806410383"}];
export function CommandPalette(){const[open,setOpen]=useState(false);const[q,setQ]=useState("");const{toggle}=useTheme();useEffect(()=>{const key=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setOpen(x=>!x)}if(e.key==="Escape")setOpen(false)};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[]);if(!open)return null;const filtered=destinations.filter(x=>x.label.toLowerCase().includes(q.toLowerCase()));return <div className="palette-backdrop" onMouseDown={()=>setOpen(false)}><div className="palette surface" role="dialog" aria-modal="true" aria-label="Command palette" onMouseDown={e=>e.stopPropagation()}><label><Search/><span className="sr-only">Search commands</span><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Where would you like to go?"/></label><div>{filtered.map(item=><a key={item.label} href={item.href} onClick={()=>setOpen(false)}>{item.label}{item.label==="GitHub"?<Github/>:item.label==="LinkedIn"?<Linkedin/>:null}</a>)}<button onClick={()=>{toggle();setOpen(false)}}>Toggle theme <Moon/></button></div></div></div>}
