"use client";
import * as SimpleIcons from "react-icons/si";import * as FontAwesome from "react-icons/fa6";import { Code2 } from "lucide-react";import { motion,useReducedMotion } from "framer-motion";import { skills } from "@/data/skills";
export function Skills(){const reduced=useReducedMotion();return <section id="skills" className="section"><div className="container"><p className="eyebrow">03 / Toolkit</p><h2 className="section-title">Technologies found<br/>across my projects.</h2><p className="section-copy">A source-verified view of the languages and tools in my public repositories—not a ranking of proficiency.</p><div className="skill-grid">{skills.map((skill,index)=>{const icons={...SimpleIcons,...FontAwesome} as Record<string,React.ComponentType>;const Icon=icons[skill.icon]??Code2;return <motion.button key={`${skill.category}-${skill.name}`} className="skill surface" style={{"--brand":skill.color} as React.CSSProperties} aria-label={`${skill.name}, ${skill.category}`} title={skill.name} initial={reduced?false:{opacity:0,y:24,scale:.94}} whileInView={{opacity:1,y:0,scale:1}} viewport={{once:false,amount:.2,margin:"-20px"}} transition={{duration:.48,delay:Math.min(index*.02,.24),ease:[.22,1,.36,1]}}><Icon/><span>{skill.name}</span><small className="mono">{skill.category}</small></motion.button>})}</div></div></section>}
