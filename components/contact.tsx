"use client";
import { useState } from "react";import { Github,Linkedin,LoaderCircle,Send } from "lucide-react";import { contactSchema } from "@/lib/contact";
type FormState={name:string;email:string;subject:string;message:string;company:string};const initial:FormState={name:"",email:"",subject:"",message:"",company:""};
export function Contact(){const[form,setForm]=useState(initial);const[state,setState]=useState<"idle"|"loading"|"success"|"error">("idle");const[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const valid=contactSchema.safeParse(form);if(!valid.success){setState("error");setMessage("Please complete every field with valid information.");return}setState("loading");try{const res=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const body=await res.json() as {message:string};setMessage(body.message);setState(res.ok?"success":"error");if(res.ok)setForm(initial)}catch{setState("error");setMessage("Something went wrong. Please try again.")}}return <section id="contact" className="section"><div className="container contact-grid"><div><p className="eyebrow">05 / Contact</p><h2 className="section-title">Let’s build something meaningful.</h2><p className="section-copy">Have an internship, junior role, university collaboration or practical project in mind? I’d be glad to hear about it.</p><div className="contact-social"><a href="https://github.com/NipulK" target="_blank" rel="noreferrer"><Github/> GitHub</a><a href="https://www.linkedin.com/in/nipul-kanishka-806410383" target="_blank" rel="noreferrer"><Linkedin/> LinkedIn</a></div></div><form className="contact-form surface" onSubmit={submit} noValidate><div className="field-row"><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} autoComplete="name" required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} autoComplete="email" required/></label></div><label>Subject<input value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} required/></label><label>Message<textarea rows={6} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} required/></label><label className="honeypot" aria-hidden="true">Company<input tabIndex={-1} autoComplete="off" value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/></label><div className="form-foot"><p role="status" className={state}>{message}</p><button className="button primary" disabled={state==="loading"}>{state==="loading"?<LoaderCircle className="spin"/>:<Send/>} Send message</button></div></form></div></section>}
