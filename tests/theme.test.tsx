import { render,screen } from "@testing-library/react";import userEvent from "@testing-library/user-event";import { beforeEach,describe,expect,it } from "vitest";import { Providers } from "@/components/providers";import { ThemeToggle } from "@/components/theme-toggle";
describe("theme switching",()=>{beforeEach(()=>localStorage.clear());it("persists the selected theme",async()=>{render(<Providers><ThemeToggle/></Providers>);await userEvent.click(screen.getByRole("button",{name:"Switch to light mode"}));expect(localStorage.getItem("nk-theme")).toBe("light");expect(document.documentElement).not.toHaveClass("dark")})});
