# Public canonical URL (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional GitHub token, server-side only; raises API rate limits
GITHUB_TOKEN=
# Contact recipient and Web3Forms key. Form stays in demo mode until configured.
CONTACT_EMAIL=
WEB3FORMS_ACCESS_KEY=
